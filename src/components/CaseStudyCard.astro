---
import { copy } from "../content/site";
import Arrow from "./Arrow.astro";
interface Props {
  id: string;
  sector: string;
  sectorLabel: string;
  headline: string;
  metric: string;
  metricLabel: string;
  description: string;
  tags: string[];
  status: string;
  index: number;
}
const cs = Astro.props;
---

<article class:list={["case-card", { "case-card-featured": cs.index === 0 }]}>
  <div class="case-meta">
    <span>{cs.sectorLabel}</span>
    <span aria-hidden="true">{String(cs.index + 1).padStart(2, "0")}</span>
  </div>
  <div class="case-result">
    <strong>{cs.metric}</strong>
    <p>{cs.metricLabel}</p>
  </div>
  <span class="outcome-label">{cs.status}</span>
  <h3>
    <a href={`/case-studies/${cs.id}/`}>{cs.headline}</a>
  </h3>
  <p class="case-description">{cs.description}</p>
  <div class="case-bottom">
    <span>{cs.tags[0]}</span>
    <a
      href={`/case-studies/${cs.id}/`}
      aria-label={`${copy.cases.read}: ${cs.headline}`}
    >
      {copy.cases.read}
      <Arrow />
    </a>
  </div>
</article>
